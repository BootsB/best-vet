<div class="container-fluid custom-appointments">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body">
          <h1 class="text-center"><%= current_user.vet? ? "Arrange Your Consultation" : "Schedule Your Appointment" %></h1>
          <%= render "form" %>
        </div>
      </div>
    </div>
  </div>
</div>
