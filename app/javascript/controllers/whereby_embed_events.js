/* const elm = document.querySelector("whereby-embed");

function logEvent(event) {
    console.log(`got event ${JSON.stringify({ type: event.type, detail: event.detail })}`);

    if (event.type === "leave") {
        console.log("Leaving the room. Redirecting to reviews page...");
        redirectToReviews();
    }
}

function redirectToReviews() {
    console.log("Redirecting to reviews page...");
    window.location.href = '<%= new_appointment_review_path %>'; // Redirect to reviews page
}

elm.addEventListener("leave", logEvent);
 */
